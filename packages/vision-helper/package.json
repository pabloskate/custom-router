{
  "name": "@custom-router/vision-helper",
  "version": "0.1.1",
  "license": "AGPL-3.0-only",
  "private": true,
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "customrouter-vision-helper": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && npm run build:standalone",
    "build:standalone": "node scripts/build-standalone.mjs",
    "prepack": "npm run build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.21.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9"
  }
}
